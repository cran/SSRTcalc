% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core_ssrt.R
\name{mean_fixedSSD}
\alias{mean_fixedSSD}
\title{Estimate SSRT via the mean method (fixed SSD design)}
\usage{
mean_fixedSSD(
  data,
  stop_col = "vol",
  rt_col = "RT_exp",
  acc_col = "correct",
  ssd_col = "soa",
  min_rt = 50
)
}
\arguments{
\item{data}{A data.frame with one row per trial.}

\item{stop_col}{Column name for the stop-trial indicator (1 = stop, 0 = go).
Default \code{"vol"}.}

\item{rt_col}{Column name for reaction time in ms. Default \code{"RT_exp"}.}

\item{acc_col}{Column name for accuracy (1 = correct). Default \code{"correct"}.}

\item{ssd_col}{Column name for stop-signal delay in ms. Default \code{"soa"}.}

\item{min_rt}{Minimum valid RT in ms; shorter responses are excluded as
anticipations. Default 50.}
}
\value{
A single numeric value: the estimated SSRT in milliseconds.
}
\description{
Computes SSRT as \eqn{\bar{RT}_{go} - \bar{SSD}}. For multiple fixed SSD
values the mean is taken across all stop trials.
}
\examples{
data(fixed)
d <- fixed[fixed$SubjID == 1, ]
mean_fixedSSD(d)

}
