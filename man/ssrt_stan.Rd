% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stan_extensions.R
\name{ssrt_stan}
\alias{ssrt_stan}
\title{Bayesian SSRT estimation via Stan}
\usage{
ssrt_stan(
  data,
  model = c("single", "hierarchical"),
  subject_col = NULL,
  trigger_failure = FALSE,
  n_quad = 100,
  chains = 4,
  iter = 2000,
  warmup = 1000,
  cores = 4,
  backend = "auto",
  adapt_delta = 0.95,
  stop_col = "vol",
  rt_col = "RT_exp",
  acc_col = "correct",
  ssd_col = "soa",
  seed = 42,
  ...
)
}
\arguments{
\item{data}{data.frame in SSRTcalc long format.}

\item{model}{\code{"single"} (default) or \code{"hierarchical"}.}

\item{subject_col}{Column with subject IDs (required for hierarchical).}

\item{trigger_failure}{Logical: include trigger-failure parameter. Default FALSE.}

\item{n_quad}{Quadrature resolution for P(inhibit) integral. Default 100.}

\item{chains}{Number of MCMC chains. Default 4.}

\item{iter}{Total iterations per chain including warmup. Default 2000.}

\item{warmup}{Warmup iterations. Default 1000.}

\item{cores}{Parallel cores. Default 4.}

\item{backend}{\code{"cmdstanr"}, \code{"rstan"}, or \code{"auto"}.}

\item{adapt_delta}{HMC target acceptance rate. Increase to 0.99 if
divergences appear. Default 0.95.}

\item{stop_col, rt_col, acc_col, ssd_col}{Column names.}

\item{seed}{Random seed. Default 42.}

\item{...}{Additional arguments passed to the Stan sampler.}
}
\value{
An object of class \code{ssrt_stan}.
}
\description{
Fits the independent horse-race model with ex-Gaussian go- and
stop-process distributions using Hamiltonian Monte Carlo via Stan.
Supports single-subject and multi-subject hierarchical designs, with an
optional trigger-failure parameter following Matzke et al. (2013).
}
\examples{
\dontrun{
data(adaptive)
d <- adaptive[adaptive$SubjID == 1, ]
fit <- ssrt_stan(d, chains = 4, iter = 2000)
print(fit)
plot(fit)

# Hierarchical
fit_h <- ssrt_stan(adaptive, model = "hierarchical",
                   subject_col = "SubjID", chains = 4)
ranef(fit_h)
}

}
\references{
Matzke, D., et al. (2013). Bayesian parametric estimation of stop-signal
reaction time distributions. \emph{Journal of Experimental Psychology:
General}, 142(4), 1047--1073.
}
